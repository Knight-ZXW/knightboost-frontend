// import { DefaultApiClient } from '@/apiclient'

import { MLApiClient } from '@/apiclient'

export default class EventGroupService {
  static getGroupList() {
    // MLApiClient.get('groupList')
    return []
  }
}
